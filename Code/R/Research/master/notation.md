1. Single channel, $N = 99$
    1. Real case
        1. $s_n = 1$
        2. $s_n = \exp(n / 50)$
            1. reconstruction
            2. parameters estimation
        3. $s_n = \cos(2 \pi n / 10)$
            1. reconstruction
            2. parameters estimation
        4. $s_n = \exp(n / 50) \cos(2 \pi n / 10)$
            1. reconstruction
            2. parameters estimation
        5. $s_n = 2 + \cos(2 \pi n / 10)$
            1. reconstruction
            2. separation
        6. $s_n = 2 \cos(2 \pi n / 5) + \cos(2 \pi n / 10)$
            1. reconstruction
            2. separation
            3. parameters estimation
        7. $s_n = 1 + n / 10$
            1. reconstruction
    2. Complex case
        1. $\exp(2 \pi \iu n / 10)$
            1. reconstruction
            2. parameters estimation
        2. $\exp(n / 50 + 2 \pi \iu n / 10)$
            1. reconstruction
            2. parameters estimation
        3. $1 + \exp(n / 50 + 2 \pi \iu n / 10)$
            1. reconstruction
            2. separation
        4. $2 \exp(2 \pi \iu n / 5) + \exp(2 \pi \iu n / 5)$
            1. reconstruction
            2. separation
            3. parameters estimation
